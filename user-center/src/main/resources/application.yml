

spring:
  application:
    name: user-center
  profiles:
    active: default
  jackson:
      default-property-inclusion: non_null

  cloud:
    config:
    enabled: true

eureka:
  instance:
  preferIpAdddress: true
  client:
    registryWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://47.105.111.80:8761/eureka/

server:
  port: 8081